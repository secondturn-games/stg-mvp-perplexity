# Second Turn Games - Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from your Supabase project dashboard
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# SITE CONFIGURATION
# =============================================================================
# Your site URL (used for auth redirects and email links)
# Development: http://localhost:3000
# Production: https://your-domain.vercel.app
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# SECURITY
# =============================================================================
# Random secret for cron job authentication (generate with: openssl rand -hex 32)
CRON_SECRET=your-random-secret-here

# =============================================================================
# MONITORING & ANALYTICS
# =============================================================================
# Vercel Analytics - Automatically enabled when deployed to Vercel
# No additional configuration needed for basic analytics

# Vercel Speed Insights - Automatically enabled when deployed to Vercel
# No additional configuration needed for Core Web Vitals tracking

# =============================================================================
# SENTRY ERROR TRACKING
# =============================================================================
# Sentry DSN for error tracking and performance monitoring
# Get this from your Sentry project settings
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Sentry configuration
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=your-sentry-project

# Sentry authentication token (for source maps upload)
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# Environment for Sentry (development, staging, production)
SENTRY_ENVIRONMENT=development

# Release version (auto-detected from git in production)
SENTRY_RELEASE=development

# =============================================================================
# LOGFLARE INTEGRATION (for Supabase logs)
# =============================================================================
# Logflare source token for sending structured logs
LOGFLARE_SOURCE_TOKEN=your-logflare-source-token

# Logflare API key for authentication
LOGFLARE_API_KEY=your-logflare-api-key

# Logflare endpoint (default: https://api.logflare.app/logs)
LOGFLARE_ENDPOINT=https://api.logflare.app/logs

# =============================================================================
# DEVELOPMENT ONLY
# =============================================================================
# These are only needed for local development
# Database direct URL (for migrations)
DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres

# =============================================================================
# PRODUCTION NOTES
# =============================================================================
# 1. Never commit .env.local to version control
# 2. Use Vercel dashboard to set production environment variables
# 3. Mark sensitive variables as "Sensitive" in Vercel
# 4. Regularly rotate secrets and API keys
# 5. Use different Supabase projects for development and production
